# Python Exercises 🐍

A growing collection of Python exercises and small projects built during 
self-study and FreeCodeCamp coursework. Each file is a standalone script 
focused on practicing a specific concept.

## Projects

### Caesar Cipher — `caesar_cipher.py`
Encrypts and decrypts text using the Caesar cipher algorithm.
Supports both uppercase and lowercase letters and validates shift input.
Built using Python's `str.maketrans()` for clean, efficient character mapping.

**Concepts:** string manipulation, input validation, higher-order functions

---

### Character Creator — `character_creator.py`
A simple RPG-style character builder that validates stat distribution
and displays attributes using a dot-based visual bar (● ○).

**Concepts:** input validation, f-strings, modular functions, Unicode output

---

### PIN Extractor — `pin_extractor.py`
Extracts a secret numeric code from a list of poems by reading the length
of a specific word on each line based on its position.

**Concepts:** nested loops, `enumerate()`, string splitting, list building

---

## About
These exercises were written while working through Python fundamentals —
loops, functions, strings, and input validation.
More projects will be added as I progress

Built with: Python 3
